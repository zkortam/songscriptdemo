/** Shared focus ring, used by every interactive primitive. */
export const focusRing =
  "focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[rgb(var(--ring)/0.55)] focus-visible:ring-offset-2 focus-visible:ring-offset-canvas";
